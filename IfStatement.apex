Integer x=10;
Integer y=20;

if(x<y){ 
    System.debug('True: x is smaller than y');  //This line is printed.
} 
if(y==20){
    System.debug('True: y is 20');   //This line is printed.
} 
if(x==y){
    System.debug('True: x is equal to y');
} 
if(x>y){
    System.debug('True: x is bigger than y');
} 
if(x==10){
    System.debug('True: x is 10');   //This line is printed.
}